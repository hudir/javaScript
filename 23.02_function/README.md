# function

```
function name(parameter1, parameter2, parameter3) {
    // code to be executed
};

const name = functionName(arguments1, arguments2, ...);
```

- default values
```
function name(parameter1 = default values, ...) {
    // some code ;
    return // some code  ===> return stop the function process and give the resault back
};
